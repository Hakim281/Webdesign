# MySQL connection string for XAMPP.
# Default XAMPP MySQL credentials are usually root with no password.
DATABASE_URL=mysql://root:@127.0.0.1:3306/student_management_system?charset=utf8mb4

# Optional fallback credentials if you prefer a PDO DSN in DATABASE_URL.
DB_USERNAME=root
DB_PASSWORD=
